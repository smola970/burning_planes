//new collection
// App.seats = new App.Collections.Seats();

// //fetch seats from back end
// // App.seats.fetch().done(function() {

// //when done fetching from back end

// App.seats.each(function(seat, index) {
//   var view = new App.Views.SeatView({ model: seat});
//     $('.seats').append(view.render().el);
//   });
// });

